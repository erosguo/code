# AGENTS.md

## Repo state

- **Starter repo** — only `README.md` (content: `hi`) and `.git/`
- Single commit `ff3681b` on `master` at `https://github.com/erosguo/code.git`
- No package manager, build system, tests, linter, CI, or source code yet

## Implications for agents

- Do not look for `package.json`, `Cargo.toml`, `Makefile`, or any tool config — none exist
- Do not attempt to run build, test, lint, or format commands
- Any work done here will likely be initial project scaffolding
